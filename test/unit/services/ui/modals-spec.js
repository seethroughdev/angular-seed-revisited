'use strict';

var svc = require('../../../../app/js/services/ui/modals-svc');

describe('Modal Service', function () {

  it('should be a valid service', function () {
    expect(svc).to.be.a('function');
  });

});
